langgraph
langchain-core
langchain-openai
python-dotenv
pydantic
typing_extensions

# ============ 新增：记忆系统增强 ============
# MySQL / ORM
SQLAlchemy>=2.0.0
PyMySQL>=1.1.0

# Redis 客户端
redis>=5.0.0

# Qdrant 向量数据库客户端
qdrant-client==1.12.2

# 通义（Embedding API + 扩展能力）
dashscope>=1.20.0

# 缓存：LRU + TTL
cachetools>=5.3.0

# 可选：HTTP 请求库（Embedding/外部 API 兜底）
httpx>=0.27.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============ 新增：语音识别模块 ============
# 麦克风采集
sounddevice>=0.4.6

# VAD（Silero）
silero-vad>=5.1

# ASR（FunASR + SenseVoice）
funasr>=1.1.0
numpy>=1.24.0

# ★transformers 必须锁定 4.51.3（5.x 与 SenseVoice/CosyVoice 不兼容，
#   会导致 KV cache 损坏、语种误判）
transformers==4.51.3

# PyTorch（CUDA 12.1）—— 需通过专用 index 安装：
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0
